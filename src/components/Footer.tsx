import { Box, VStack } from "@chakra-ui/react";

export default function Footer() {
    return (
        <Box width={"100%"}>
            <VStack h={"120px"} bg={"gray.100"}>
                <address>address</address>
            </VStack>
        </Box>
    );
}